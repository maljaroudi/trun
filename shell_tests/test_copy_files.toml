#! ../target/release/trun
["COPY TO HOME"]
module = "File"
name = "Copy Cargo.lock to CargoCopy0"
src = "~! SRCS !~"
dest = "~! DESTS !~"
items = { SRCS = ["./Cargo.lock"], DESTS = ["./CargoCopy0"] }
check = true
move = true
